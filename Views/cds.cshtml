<h1>CDs</h1>
@if (@Model.Count == 0)
{
  <h3>No Cds in your collection!</h3>
}
@foreach (var cd in Model)
{
  <h3></cds/@cd.GetId()>@cd.GetDescription()</h3>
  <h3></cds/@cd.GetId()>@cd.GetArtist()</h3>
}
<p><a href="/">Back Home</a></p>
